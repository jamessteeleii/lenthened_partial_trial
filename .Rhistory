10*15
30*15
install.packages("ggdag")
arm_ma = (b0 + u_site + u_participant) + (b_time * time_cont) + (b_cond * cond_dummy) + (b_cond_time * cond_dummy * time_cont) + sigma
library(ggdag)
dagified <- dagify(x ~ z,
y ~ z,
exposure = "x",
outcome = "y"
)
tidy_dagitty(dagified)
ggdag(dagified)
ggdag(dagified, layout("circle"))
ggdag(dagified, layout="circle")
ggdag(dagified, layout= "circle") +
theme_dag_grid()
ggdag(dagified, layout= "circle") +
theme_dag_blank()
dagified <- dagify(
y ~ (b0 + u_site + u_participant) + (b_time * time_cont) + (b_cond * cond_dummy) + (b_cond_time * cond_dummy * time_cont) + sigma,
# y ~ z,
exposure = "cond_dummy",
outcome = "y"
)
dagified <- dagify(
y_1 ~ b0 + u_site + u_participant + sigma,
y_2 ~ b0 + u_site + u_participant + sigma + b_time * time_cont + b_cond * cond_dummy + b_cond_time * cond_dummy * time_cont
# y ~ z,
exposure = "cond_dummy",
dagified <- dagify(
y_1 ~ b0 + u_site + u_participant + sigma,
y_2 ~ b0 + u_site + u_participant + sigma + b_time * time_cont + b_cond * cond_dummy + b_cond_time * cond_dummy * time_cont
exposure = "cond_dummy",
dagified <- dagify(
y_1 ~ b0 + u_site + u_participant + sigma,
y_2 ~ b0 + u_site + u_participant + sigma + b_time * time_cont + b_cond * cond_dummy + b_cond_time * cond_dummy * time_cont,
exposure = "cond_dummy",
outcome = "y"
)
dagified <- dagify(
y_1 ~ u_site + u_participant + sigma,
y_2 ~ u_site + u_participant + sigma + time_cont +  cond_dummy + cond_dummy:time_cont,
exposure = "cond_dummy",
outcome = "y"
)
dagified <- dagify(
y_1 ~ u_site + u_participant + sigma,
y_2 ~ u_site + u_participant + sigma + time_cont +  cond_dummy + `cond_dummy:time_cont`,
exposure = "cond_dummy",
outcome = "y"
)
dagified <- dagify(
y_1 ~ u_site + u_participant + sigma,
y_2 ~ u_site + u_participant + sigma + time_cont +  cond_dummy + cond_dummy:time_cont,
exposure = "cond_dummy",
outcome = "y"
)
dagified <- dagify(
y_1 ~ u_site + u_participant + sigma,
y_2 ~ u_site + u_participant + sigma + time_cont +  cond_dummy + cond_dummy_time_cont,
exposure = "cond_dummy",
outcome = "y"
)
dagified <- dagify(
y_1 ~ u_site + u_participant + sigma,
y_2 ~ u_site + u_participant + sigma + time_cont +  cond_dummy + cond_dummy_time_cont,
exposure = "cond_dummy",
outcome = "y_2"
)
ggdag(dagified, layout = "circle") +
theme_dag_blank()
dagified <- dagify(
y_1 ~ u_site + u_participant + sigma,
y_2 ~ y_1 + time_cont +  cond_dummy + cond_dummy_time_cont + sigma,
exposure = "cond_dummy",
outcome = "y_2"
)
tidy_dagitty(dagified)
ggdag(dagified, layout = "circle") +
theme_dag_blank()
ggdag_paths(dagified, layout = "circle") +
theme_dag_blank()
ggdag(dagified, layout = "circle") +
theme_dag_blank()
bigger_dag <- dag_paths(dagified)
ggdag(bigger_dag, layout = "circle") +
theme_dag_blank()
ggdag_parents(bigger_dag, layout = "circle") +
theme_dag_blank()
bigger_dag <- dag_paths(dagified) %>%
group_by(set) %>%
filter(!is.na(path) & !is.na(name)) %>%
summarize(n_vars_between = n() - 1L)
library(tidyverse)
bigger_dag <- dag_paths(dagified) %>%
group_by(set) %>%
filter(!is.na(path) & !is.na(name)) %>%
summarize(n_vars_between = n() - 1L)
ggdag_parents(bigger_dag, layout = "circle") +
theme_dag_blank()
ggdag_parents(bigger_dag, "x") +
theme_dag_blank()
bigger_dag
bigger_dag <- dag_paths(dagified)
ggdag_parents(bigger_dag, "x") +
theme_dag_blank()
ggdag_parents(bigger_dag, "cond_dummy") +
theme_dag_blank()
